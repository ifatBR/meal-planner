| Module | Method | Endpoint                | Description                    | Path Params | Query Params | Request                                                                      | Response                                                                     | Auth Required | Status Codes | Notes                                                                                                                                                  |
| ------ | ------ | ----------------------- | ------------------------------ | ----------- | ------------ | ---------------------------------------------------------------------------- | ---------------------------------------------------------------------------- | ------------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| auth   | POST   | /api/v1/auth/login      | Login user                     | -           | -            | { email:string(email), password:string(min:8,max:128), rememberMe?:boolean } | { data:{ user:{ id:string,email:string,name:string }, accessToken:string } } | no            | 200,400,401  | Validates credentials, creates refresh session, sets refresh token cookie for web, access token lifetime ~15 min                                       |
| auth   | POST   | /api/v1/auth/refresh    | Refresh access token           | -           | -            | -                                                                            | { data:{ accessToken:string } }                                              | no            | 200,401      | Reads refresh token from cookie, validates active refresh session, returns new access token, refresh token lifetime 30–90 days depending on rememberMe |
| auth   | POST   | /api/v1/auth/logout     | Logout current session         | -           | -            | -                                                                            | { data:{ success:true } }                                                    | no            | 200          | Reads refresh token from cookie, invalidates that refresh session if it exists, clears refresh cookie, always safe to call                             |
| auth   | GET    | /api/v1/auth/me         | Get current authenticated user | -           | -            | -                                                                            | { data:{ user:{ id:string,email:string,name:string } } }                     | yes           | 200,401      | Uses access token from Authorization header                                                                                                            |
| auth   | POST   | /api/v1/auth/logout-all | Logout all user sessions       | -           | -            | -                                                                            | { data:{ success:true } }                                                    | yes           | 200,401      | Optional for MVP. Uses access token, deletes all refresh sessions for current user, clears refresh cookie if present                                   |
